import 'package:flutter/material.dart';

class ClubAboutPage extends StatelessWidget {
  const ClubAboutPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("About Page"),
    );
  }
}
